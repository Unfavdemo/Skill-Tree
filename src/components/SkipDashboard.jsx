import React from "react";
import SkillDashboard from "./SkillDashboard";

const SkipDashboard = () => {
  return <SkillDashboard showResumeSkipped={true} showUploadButton={true} />;
};

export default SkipDashboard;
